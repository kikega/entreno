from django.apps import AppConfig


class TrainingConfig(AppConfig):
    """Configuración de la aplicación de gestión de entrenamientos y analíticas."""
    default_auto_field = "django.db.models.BigAutoField"
    name = "training"

